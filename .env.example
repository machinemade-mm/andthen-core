# And Then? Core - Environment Variables
# Copy this file to .env and fill in your actual values
# NEVER commit .env to git!

# Database (PostgreSQL)
# Replace with your local PostgreSQL credentials
DATABASE_URL=postgresql://username:password@localhost:5432/andthen

# JWT Authentication
# Generate a random secret for production (e.g., openssl rand -base64 32)
JWT_SECRET=your-super-secret-jwt-key-change-this
